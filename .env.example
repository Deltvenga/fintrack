# Vercel Blob token (auto-injected when Blob store is linked to the project)
BLOB_READ_WRITE_TOKEN=

# Blob access mode: "private" (default) or "public" — must match your Blob store type
# BLOB_ACCESS=private

# Local development uses data/db.json when BLOB_READ_WRITE_TOKEN is empty
